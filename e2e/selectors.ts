/**
 * E2E test selectors — re-exports the stable contract.
 * Tests import from here; frontend imports from src/test/selectors.ts.
 * Both stay in sync automatically.
 */
export { Selectors } from "../src/test/selectors";